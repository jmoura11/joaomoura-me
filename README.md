# Project Setup

## Running Server Tests

1. Navigate to the `server` directory.
2. Install dependencies (only needed once):
   ```bash
   npm install
   ```
3. Run the tests:
   ```bash
   npm test
   ```

The tests start the server in test mode and validate the `POST /api/v1/dalle` route.
